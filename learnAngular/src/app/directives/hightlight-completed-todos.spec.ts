import { HightlightCompletedTodos } from './hightlight-completed-todos';

describe('HightlightCompletedTodos', () => {
  it('should create an instance', () => {
    const directive = new HightlightCompletedTodos();
    expect(directive).toBeTruthy();
  });
});
